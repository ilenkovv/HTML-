import type { DatasetStats } from "@/types/aiData";
export interface AiDataPanelProps { stats: DatasetStats; collectionEnabled: boolean; allowedEventTypes: string[]; latestDatasetVersion?: number | null; busy?: boolean; onToggleCollection: (enabled: boolean) => void; onCreateDataset: () => void; onDownloadLatest: () => void; }
export function AiDataPanel(props: AiDataPanelProps) {
  const cards = [["Собрано", props.stats.total], ["Нужно проверить", props.stats.raw], ["Проверено", props.stats.verified], ["Готово для ИИ", props.stats.trainingReady]] as const;
  return <section className="space-y-4 rounded-2xl border bg-card p-4 sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="text-xl font-semibold">База ИИ</h2><p className="mt-1 max-w-2xl text-sm text-muted-foreground">Ваши подтверждённые данные сохраняются отдельно и могут быть выгружены в переносимом формате. Пароли, токены и другие секреты система не принимает.</p></div><label className="flex items-center gap-2 text-sm font-medium"><input type="checkbox" checked={props.collectionEnabled} onChange={(event) => props.onToggleCollection(event.currentTarget.checked)} disabled={props.busy}/>Собирать разрешённые события</label></div><div className="grid grid-cols-2 gap-2 lg:grid-cols-4">{cards.map(([label, value]) => <div key={label} className="rounded-xl border bg-background p-3"><div className="text-xs text-muted-foreground">{label}</div><div className="mt-1 text-2xl font-semibold tabular-nums">{value}</div></div>)}</div><div className="rounded-xl bg-muted p-3 text-sm"><div className="font-medium">Разрешённые события</div><div className="mt-1 text-muted-foreground">{props.allowedEventTypes.length > 0 ? props.allowedEventTypes.join(", ") : "Не заданы — автоматический сбор не ведётся"}</div></div><div className="flex flex-col gap-2 sm:flex-row"><button type="button" className="h-11 rounded-xl bg-primary px-4 font-medium text-primary-foreground disabled:opacity-50" onClick={props.onCreateDataset} disabled={props.busy || props.stats.trainingReady === 0}>Создать версию набора</button><button type="button" className="h-11 rounded-xl border bg-background px-4 font-medium disabled:opacity-50" onClick={props.onDownloadLatest} disabled={props.busy || !props.latestDatasetVersion}>Скачать ZIP{props.latestDatasetVersion ? ` · v${props.latestDatasetVersion}` : ""}</button></div><p className="text-xs text-muted-foreground">В обучение попадают только записи со статусом «Готово для ИИ». Обычные сырые события автоматически туда не включаются.</p></section>;
}
